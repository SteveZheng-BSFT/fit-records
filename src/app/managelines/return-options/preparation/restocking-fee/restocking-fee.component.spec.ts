import { TestBed } from '@angular/core/testing';
import { RestockingFeeComponent } from './restocking-fee.component';

describe('RestockingFeeComponent', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            declarations: [ RestockingFeeComponent ]
        });
    });

    describe('method ', () => {
        xit('should ', () => {
        });
    });

    xit('should ', () => {
    });
});
